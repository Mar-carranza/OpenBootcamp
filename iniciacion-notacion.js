//Notacion

//; ===> Delimita el final de una linea. Aunque ya no es imprescindible es una buena practica
const b = 4;

//. ===> Se utiliza en los objetos para acceder a los atributos
//movil.anchura

//[] ===> Listas, arrays
const array = [1, 2, 3]
console.log(array[2])

//() ===> funciones
function suma(a,b) {
    //aqui va la funcion
}
//{} ===> Objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if (true){
    //todo lo que haya entre llave
    const constante2 = 'Hola'
}